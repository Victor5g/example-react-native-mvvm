import MockAdapter from 'axios-mock-adapter';
import client from '../../../../src/repositories/client';

const mock = new MockAdapter(client);

describe("Unit-Test: useLoginViewModel() ", () => {
  it("should be able to sign in", async () => {
   
  });
});